import { steps, ambient } from '../fourg/sms.js';

export const label = 'SMS';
export const blurb = 'Identical to 4G — the MME anchors SMS delivery whether or not a secondary gNB is attached';

export { steps, ambient };
